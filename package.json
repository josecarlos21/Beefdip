{
  "name": "vallarta-live-|-visual-experience",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "generate-theme": "node scripts/generate-theme.cjs",
    "dev": "npm run generate-theme && vite",
    "build": "npm run generate-theme && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.2.3",
    "react-dom": "^19.2.3",
    "react-router-dom": "^7.11.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
